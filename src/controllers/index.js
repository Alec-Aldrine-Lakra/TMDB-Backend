"use strict";
const {
  loginAccountHandler,
  logoutHandler,
  forgotPasswordHandler,
  verifyPasswordHandler,
} = require("./users.controller");

module.exports = {
  loginAccountHandler,
  logoutHandler,
  forgotPasswordHandler,
  verifyPasswordHandler,
};
